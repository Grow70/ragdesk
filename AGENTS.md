# 项目协作规则

本项目是用于申请 Agent 开发实习的企业知识库问答系统。协作目标是完成项目，同时让项目负责人能够解释代码、设计取舍、测试和实验结果。

## 每步工作方式

1. 项目负责人每次给出一个编号任务时，只完成当前任务，不自动进入下一步。
2. 开始前阅读 `AGENTS.md`、`docs/requirements.md`、`docs/architecture.md` 和 `docs/progress.md`；不存在的文件应明确说明，不得假装读过。
3. 执行前先说明本步目标、依赖、拟修改文件和验收方式。
4. 每步只交付一个主要能力。若预计涉及超过 6 个主要实现文件，先拆成更小任务，本次只做第一个能独立验收的部分。测试、锁文件和生成文件不计入这个软限制。
5. 不重写整个项目，不引入与当前步骤无关的依赖，不提前实现后续功能。
6. 涉及权限、检索算法、数据一致性和错误处理时，先明确测试用例，再实现；不要为了测试数量写没有实际价值的测试。
7. 查询所用依赖的当前官方文档，采用兼容版本并记录锁文件；无法联网时明确说明，不猜测 API。
8. 单元测试默认使用可控的 fake 模型。真实模型验证单独运行，不能把 fake 测试当作真实 RAG 效果。
9. 密钥只从环境变量读取；不要把 `.env`、上传资料、数据库文件或密钥提交到 Git。
10. 不能伪造运行结果。未执行、执行失败和已通过必须区分。
11. 遇到错误先复现并定位原因，不连续随机改动。两轮修复仍无进展时，记录证据和阻塞点，停止扩大修改范围。
12. 每步结束更新 `docs/progress.md`：已完成、验证结果、遗留问题、下一步入口。
13. 不自动 push、部署到公网或调用大量收费 API。

## 每步交付内容

- 改动与原因；
- 实际执行的验证及结果；
- 可以复制的启动或验收命令；
- 一个与本步有关的计算机知识点；
- 两个面试追问及回答要点；
- 建议的 Git commit message。
